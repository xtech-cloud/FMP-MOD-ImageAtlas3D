
using Grpc.Core;
using System.Threading.Tasks;
using XTC.FMP.MOD.ImageAtlas3D.LIB.Proto;

namespace XTC.FMP.MOD.ImageAtlas3D.App.Service
{
    public class DesignerService : DesignerServiceBase
    {
        // 解开以下代码的注释，可支持数据库操作
        /*
        private readonly YourDAO yourDAO_;

         /// <summary>
        /// 构造函数
        /// </summary>
        /// <remarks>
        /// 支持多个参数，均为自动注入，注入点位于MyProgram.PreBuild
        /// </remarks>
        /// <param name="_yourDAO">自动注入的数据操作对象</param>
        public DesignerService(YourDAO _yourDAO)
        {
            yourDAO_ = _yourDAO;
        }
        */
    }
}
